# Base de datos PostgreSQL
DATABASE_URL="postgresql://usuario:password@localhost:5432/clinica_db?schema=public"

# NextAuth configuración
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="tu-secret-aleatorio-muy-seguro-cambiar-en-produccion"

# Nota: Para producción, cambia NEXTAUTH_URL por tu dominio real
# y genera un NEXTAUTH_SECRET seguro con: openssl rand -base64 32